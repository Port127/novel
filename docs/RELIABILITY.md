# RELIABILITY

本仓库的可靠性护栏。

## 目标
- 保持命令行为可预测。
- 优先使用确定性检查，再做定性检查。
- 让失败模式显式且可恢复。

## 控制措施
- 在 skill 文档中定义清晰的输入/输出契约。
- 缺失前置条件时快速失败（例如缺失当前项目）。
- 保持状态写入显式且可追溯。

## 验证清单
- [ ] 契约链接可解析
- [ ] 命令执行前所需文件存在
- [ ] 缺失数据路径返回可操作的错误